{
  "name": "liga-dark-mode",
  "version": "2.0.0",
  "private": true,
  "description": "Extensao do Chrome que aplica modo escuro nos sites da Liga (LMCorp)",
  "type": "module",
  "scripts": {
    "fetch": "node scripts/fetch-css.mjs",
    "measure": "node scripts/measure-bg-images.mjs",
    "build": "node scripts/gen.mjs",
    "manifest": "node scripts/gen-manifest.mjs",
    "theme": "npm run fetch && npm run measure && npm run build && npm run manifest && npm run check",
    "check": "node scripts/check.mjs",
    "test": "node scripts/test-apply.mjs",
    "icons": "node scripts/make-icons.mjs",
    "audit": "node scripts/audit-live.mjs",
    "store": "node scripts/store-assets.mjs"
  },
  "devDependencies": {
    "postcss": "^8.4.47",
    "puppeteer-core": "^23.11.1"
  },
  "license": "MIT"
}
